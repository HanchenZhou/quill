{
  "name": "@quill/vault-adapter",
  "version": "0.0.1",
  "private": true,
  "description": "Quill vault storage abstraction (LocalProvider + future RemoteProvider / FileSystemAccessProvider)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@quill/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.6.3"
  }
}
